# This Dockerfile is never built. It exists only so that Dependabot (docker
# ecosystem) can track new envoyproxy/envoy releases and open version bump PRs.
# The tag below is kept in sync with appVersion in Chart.yaml by the
# .github/workflows/dependabot-envoy-sync.yml workflow.
FROM envoyproxy/envoy:v1.38.1
