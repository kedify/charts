
{{- if or .Values.optionalResources.all.enabled .Values.optionalResources.scalingPolicy.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-scaling-policy-metrics
  namespace: {{ .Release.Namespace }}
data:
  so-count: "8"
  sj-count: "12"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-scaling-policy-target
  namespace: {{ .Release.Namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scaling-policy-target
  template:
    metadata:
      labels:
        app: scaling-policy-target
    spec:
      containers:
        - name: nginx-container
          image: nginx:latest
          ports:
            - containerPort: 80
---
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: {{ .Release.Name }}-scaling-policy-so
  namespace: {{ .Release.Namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Release.Name }}-scaling-policy-target
  pollingInterval: 30
  cooldownPeriod: 5
  minReplicaCount: 0
  maxReplicaCount: 7
  triggers:
    - type: kubernetes-resource
      metadata:
        resourceKind: ConfigMap
        resourceName: {{ .Release.Name }}-scaling-policy-metrics
        key: so-count
        format: number
        valueType: int64
        targetValue: "1"
---
apiVersion: keda.sh/v1alpha1
kind: ScaledJob
metadata:
  name: {{ .Release.Name }}-scaling-policy-sj
  namespace: {{ .Release.Namespace }}
spec:
  jobTargetRef:
    template:
      spec:
        containers:
          - name: busybox-worker
            image: busybox
            args:
              - /bin/sh
              - -c
              - sleep 30
        restartPolicy: Never
  pollingInterval: 30
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 5
  minReplicaCount: 0
  maxReplicaCount: 7
  triggers:
    - type: kubernetes-resource
      metadata:
        resourceKind: ConfigMap
        resourceName: {{ .Release.Name }}-scaling-policy-metrics
        key: sj-count
        format: number
        valueType: int64
        targetValue: "10"
---
apiVersion: keda.kedify.io/v1alpha1
kind: ScalingPolicy
metadata:
  name: {{ .Release.Name }}-scalingpolicy-sample
  namespace: {{ .Release.Namespace }}
spec:
  targets:
    - apiVersion: keda.sh/v1alpha1
      kind: ScaledObject
      name: {{ .Release.Name }}-scaling-policy-so
    - apiVersion: keda.sh/v1alpha1
      kind: ScaledJob
      name: {{ .Release.Name }}-scaling-policy-sj
  actions:
    - name: business-hours
      priority: 1
      trigger:
        schedule:
          start: "30 6 * * *"
          end: "0 14 * * *"
          timezone: "Europe/Prague"
      adjustment:
        pollingInterval: 10
        minReplicaCount: 5
        maxReplicaCount: 7
    - name: off-hours
      priority: 1
      trigger:
        schedule:
          start: "0 18 * * *"
          end: "0 6 * * *"
          timezone: "Europe/Prague"
      adjustment:
        pollingInterval: 30
        minReplicaCount: 0
        maxReplicaCount: 2
    - name: peak
      priority: 2
      trigger:
        schedule:
          start: "0 9 * * *"
          end: "0 11 * * *"
          timezone: "Europe/Prague"
      adjustment:
        pollingInterval: 5
        minReplicaCount: 7
        maxReplicaCount: 7
{{- end }}
