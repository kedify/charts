{{- if and .Values.metricsServer.enabled .Values.networkPolicy.enabled (eq .Values.networkPolicy.flavor "cilium") (ne .Values.kedify.multitenant.mode "tenant") }}
apiVersion: "cilium.io/v2"
kind: CiliumNetworkPolicy
metadata:
  name: {{ (include "keda.operator.name" .) }}-metrics-apiserver
  namespace: {{ .Release.Namespace }}
  {{- with .Values.additionalAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  labels:
    app: {{ (include "keda.operator.name" .) }}-metrics-apiserver
    app.kubernetes.io/name: {{ (include "keda.operator.name" .) }}-metrics-apiserver
    {{- include "keda.labels" . | indent 4 }}
spec:
  endpointSelector:
    matchLabels:
      app: {{ (include "keda.operator.name" .) }}-metrics-apiserver
  egress:
    - toEntities:
        - kube-apiserver
        - cluster
  ingress:
    - fromEntities:
        - cluster
{{ end }}
